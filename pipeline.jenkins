#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent {
    label 'master'
    }

    options {
    timestamps()
    }

    stages {
        stage("clean folder for project") {
            steps {
                sh 'ssh ubuntu@ec2-35-163-141-59.us-west-2.compute.amazonaws.com'
            }
        }
    }
}
